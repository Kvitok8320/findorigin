# Бот не отвечает на сообщения - Диагностика

## 🔍 Пошаговая проверка

### Шаг 1: Проверка webhook

Проверьте, установлен ли webhook на ваш Vercel URL:

```bash
npm run check-webhook
```

**Должно показать:**
- `url`: `https://findorigin.vercel.app/api/telegram`
- `pending_update_count`: 0 или небольшое число
- `last_error_date`: отсутствует (или очень старое значение)

**Если webhook не установлен или неправильный:**
```bash
npm run set-webhook
```

Или вручную:
```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"https://findorigin.vercel.app/api/telegram\"}"
```

### Шаг 2: Проверка переменных окружения на Vercel

1. Зайдите на https://vercel.com
2. Откройте проект **findorigin**
3. Перейдите в **Settings** → **Environment Variables**
4. Убедитесь, что есть:
   - `TELEGRAM_BOT_TOKEN` = ваш токен бота
   - `TELEGRAM_API_URL` = `https://api.telegram.org/bot` (опционально)

**⚠️ ВАЖНО:** После добавления/изменения переменных окружения нужно **передеплоить** проект!

### Шаг 3: Проверка endpoint

Откройте в браузере:
```
https://findorigin.vercel.app/api/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "hasTelegramToken": true,
  "message": "Bot is configured correctly"
}
```

Если `hasTelegramToken: false` - проблема с переменными окружения!

### Шаг 4: Проверка логов на Vercel

1. Зайдите на Vercel → ваш проект
2. **Deployments** → выберите последний деплой
3. **Functions** → `/api/telegram`
4. Проверьте логи

**Что искать:**

✅ **Хорошие признаки:**
- Видны записи `[WEBHOOK] Received request`
- Видны записи `[WEBHOOK] Update received`
- Видны записи `[PROCESS] Starting update processing`
- Видны записи `[TELEGRAM] Message sent successfully`

❌ **Проблемы:**
- `TELEGRAM_BOT_TOKEN is not set` - не настроена переменная окружения
- `Fetch failed` или `Fetch timeout` - проблема с подключением к Telegram API
- `Telegram API error` - ошибка от Telegram API (неправильный токен, неправильный chat_id и т.д.)
- Нет записей `[WEBHOOK] Received request` - webhook не работает или не установлен

### Шаг 5: Проверка токена

```bash
npm run test-bot
```

**Должно показать:**
- Информацию о боте (id, username, first_name)

Если не работает - проблема с токеном!

### Шаг 6: Тест отправки сообщения

Отправьте боту сообщение в Telegram и сразу проверьте логи на Vercel.

**Что должно произойти:**
1. В логах появится `[WEBHOOK] Received request`
2. В логах появится `[WEBHOOK] Update received`
3. В логах появится `[PROCESS] Starting update processing`
4. В логах появится `[PROCESS] Sending initial message to chat: ...`
5. В логах появится `[TELEGRAM] Message sent successfully`
6. Бот должен ответить "🔍 Анализирую запрос..."

## 🐛 Частые проблемы и решения

### Проблема 1: Webhook не установлен

**Симптомы:**
- Бот не отвечает
- В логах нет записей `[WEBHOOK] Received request`

**Решение:**
```bash
npm run set-webhook
```

### Проблема 2: Переменные окружения не настроены

**Симптомы:**
- В логах: `TELEGRAM_BOT_TOKEN is not set`
- `/api/health` показывает `hasTelegramToken: false`

**Решение:**
1. Добавьте `TELEGRAM_BOT_TOKEN` в Vercel → Settings → Environment Variables
2. **Передеплойте проект!**

### Проблема 3: Fetch timeout или ошибка подключения

**Симптомы:**
- В логах: `Fetch failed` или `Fetch timeout`
- В логах: `TypeError: fetch failed`

**Возможные причины:**
- Проблемы с сетью на Vercel
- Telegram API недоступен
- Неправильный URL API

**Решение:**
- Проверьте, что `TELEGRAM_API_URL` = `https://api.telegram.org/bot`
- Подождите несколько минут и попробуйте снова

### Проблема 4: Ошибка от Telegram API

**Симптомы:**
- В логах: `Telegram API error: ...`
- В логах: `Error response: ...`

**Возможные причины:**
- Неправильный токен
- Неправильный chat_id
- Бот заблокирован пользователем
- Превышен лимит запросов

**Решение:**
- Проверьте токен: `npm run test-bot`
- Убедитесь, что бот не заблокирован пользователем
- Подождите несколько минут (если превышен лимит)

### Проблема 5: Webhook secret не совпадает

**Симптомы:**
- В логах: `Invalid webhook secret token`
- Webhook возвращает 401

**Решение:**
- Либо уберите `WEBHOOK_SECRET` из переменных окружения
- Либо установите webhook с правильным secret_token:
```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"https://findorigin.vercel.app/api/telegram\", \"secret_token\": \"ВАШ_SECRET\"}"
```

## 📋 Чеклист диагностики

- [ ] Webhook установлен и указывает на правильный URL
- [ ] `TELEGRAM_BOT_TOKEN` настроен в Vercel
- [ ] Проект передеплоен после изменения переменных окружения
- [ ] `/api/health` показывает `hasTelegramToken: true`
- [ ] `npm run test-bot` работает
- [ ] В логах Vercel видны записи `[WEBHOOK] Received request` при отправке сообщения
- [ ] В логах нет ошибок `TELEGRAM_BOT_TOKEN is not set`
- [ ] В логах нет ошибок `Fetch failed` или `Fetch timeout`
- [ ] В логах нет ошибок `Telegram API error`

## 🆘 Если ничего не помогло

1. **Соберите информацию:**
   - Результат `npm run check-webhook`
   - Результат `npm run test-bot`
   - Результат `/api/health`
   - Логи из Vercel (последние 50 строк)

2. **Проверьте:**
   - Правильность токена (скопирован полностью, без пробелов)
   - Правильность URL webhook (должен быть `https://findorigin.vercel.app/api/telegram`)
   - Что проект задеплоен (последний деплой успешен)

3. **Попробуйте:**
   - Переустановить webhook
   - Передеплоить проект
   - Подождать 5-10 минут и попробовать снова


# Изменения: Убран анализ текста, добавлен AI и Google Search API

## Что было сделано:

### 1. Убран предварительный анализ текста
- ❌ Удален вызов `analyzeText()` из обработки сообщений
- ❌ Убрана генерация поисковых запросов на основе анализа
- ✅ Теперь поиск выполняется напрямую по исходному тексту

### 2. Добавлен AI сравнение через OpenAI GPT-4o-mini
- ✅ Создан модуль `lib/ai-comparison.ts`
- ✅ Реализована функция `compareWithAI()` для сравнения источников с исходным текстом
- ✅ Используется модель `gpt-4o-mini` с температурой 0.3
- ✅ AI оценивает релевантность (0-100%), уверенность (high/medium/low) и дает объяснение

### 3. Подключен Google Search API
- ✅ Google Search API уже был подключен в `lib/search-apis/google-search.ts`
- ✅ Используется через функцию `searchSources()` в `lib/source-searcher.ts`
- ✅ Приоритет: Google → Bing → SerpAPI

### 4. Обновлен процесс обработки сообщений

**Старый процесс:**
1. Анализ текста → генерация поисковых запросов
2. Поиск по нескольким запросам
3. Показ топ-3 результатов

**Новый процесс:**
1. Поиск по исходному тексту через Google Search API
2. AI сравнение найденных источников с исходным текстом
3. Ранжирование по релевантности
4. Показ топ-3 источников с оценкой релевантности и объяснением

### 5. Обновлены сообщения пользователю

- "🔍 Анализирую запрос..." → остается
- "🔎 Ищу возможные источники через Google Search..." → обновлено
- "🤖 Сравниваю источники с исходным текстом через AI..." → новое
- Результаты показывают:
  - Релевантность (0-100%)
  - Уверенность (high/medium/low)
  - Краткое объяснение на русском

## Требуемые переменные окружения:

### Обязательные:
- `TELEGRAM_BOT_TOKEN` - токен бота
- `OPENAI_API_KEY` - ключ OpenAI API (для AI сравнения)
- `GOOGLE_API_KEY` - ключ Google Custom Search API
- `GOOGLE_SEARCH_ENGINE_ID` - ID поисковой машины Google

### Опциональные (fallback):
- `BING_API_KEY` - если Google не работает
- `SERPAPI_KEY` - альтернативный поисковый API

## Что нужно сделать:

1. **Настроить OpenAI API:**
   - Получить ключ на https://platform.openai.com/api-keys
   - Добавить `OPENAI_API_KEY` в Vercel
   - См. `OPENAI_SETUP.md` для подробностей

2. **Настроить Google Search API:**
   - Убедиться, что `GOOGLE_API_KEY` и `GOOGLE_SEARCH_ENGINE_ID` установлены
   - Если ошибка 403 - см. `FINAL_FIX_403_ERROR.md`

3. **Передеплоить проект:**
   - После добавления переменных окружения нужно передеплоить

## Преимущества нового подхода:

- ✅ Более точное сравнение через AI
- ✅ Оценка релевантности каждого источника
- ✅ Объяснение на русском языке
- ✅ Меньше шагов обработки (без предварительного анализа)
- ✅ Прямой поиск по исходному тексту

## Fallback:

Если OpenAI API не настроен или произошла ошибка:
- Бот покажет первые 3 результата без AI оценки
- Релевантность будет 50%, уверенность "medium"


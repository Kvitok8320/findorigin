# Настройка Telegram Mini App для FindOrigin

## Что такое Telegram Mini App?

Telegram Mini App (TMA) - это веб-приложение, которое открывается внутри Telegram и предоставляет удобный UI для взаимодействия с ботом.

## Структура

- **Страница TMA**: `/app/mini-app/page.tsx` - основной UI
- **API endpoint**: `/app/api/mini-app/analyze/route.ts` - обработка запросов
- **URL**: `https://findorigin.vercel.app/mini-app`

## Настройка в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота (FindOrigin)
4. Выберите "Bot Settings" → "Menu Button"
5. Выберите "Configure Menu Button"
6. Введите:
   - **Button text**: `🔍 Найти источники` (или любое другое название)
   - **URL**: `https://findorigin.vercel.app/mini-app`

## Функциональность

### Основные возможности:

1. **Ввод текста**: Пользователь может ввести текст или ссылку на Telegram-пост
2. **Поиск источников**: При нажатии на кнопку "🔍 Найти источники" запускается поиск
3. **Отображение результатов**: Показываются топ-3 источника с:
   - Заголовком и URL
   - Оценкой релевантности (0-100%)
   - Уровнем уверенности (high/medium/low)
   - Кратким объяснением
   - Сниппетом из источника

### UI особенности:

- **Адаптивный дизайн**: Работает на мобильных и десктопах
- **Темная/светлая тема**: Автоматически подстраивается под тему Telegram
- **Haptic Feedback**: Тактильная обратная связь при действиях
- **Кнопка "Назад"**: Возврат в бота
- **Кнопка "Найти источники"**: Основная кнопка действия (внизу экрана)

## API Endpoint

### POST `/api/mini-app/analyze`

**Request:**
```json
{
  "text": "Текст для анализа",
  "initData": "Telegram Web App init data (опционально)"
}
```

**Response:**
```json
{
  "results": [
    {
      "title": "Заголовок источника",
      "url": "https://example.com/article",
      "snippet": "Краткое описание...",
      "relevanceScore": 90,
      "confidence": "high",
      "explanation": "Источник подтверждает...",
      "sourceType": "news"
    }
  ],
  "count": 3
}
```

## Интеграция с существующим ботом

TMA использует те же API и функции, что и основной бот:
- `searchSources()` - поиск через Yandex/Google/Bing
- `compareWithAI()` - AI сравнение через OpenRouter/OpenAI
- `selectTopSources()` - выбор топ-3 результатов

## Безопасность

⚠️ **Важно**: В продакшене рекомендуется добавить валидацию `initData` от Telegram для проверки подлинности запросов.

Пример валидации (можно добавить в `/api/mini-app/analyze/route.ts`):
```typescript
import crypto from 'crypto';

function validateInitData(initData: string, botToken: string): boolean {
  // Парсим initData
  const params = new URLSearchParams(initData);
  const hash = params.get('hash');
  params.delete('hash');
  
  // Создаем строку для проверки
  const dataCheckString = Array.from(params.entries())
    .sort(([a], [b]) => a.localeCompare(b))
    .map(([key, value]) => `${key}=${value}`)
    .join('\n');
  
  // Вычисляем секретный ключ
  const secretKey = crypto
    .createHmac('sha256', 'WebAppData')
    .update(botToken)
    .digest();
  
  // Вычисляем хеш
  const calculatedHash = crypto
    .createHmac('sha256', secretKey)
    .update(dataCheckString)
    .digest('hex');
  
  return calculatedHash === hash;
}
```

## Тестирование

1. **Локально**: 
   ```bash
   npm run dev
   ```
   Откройте `http://localhost:3000/mini-app` в браузере (без Telegram API)

2. **В Telegram**:
   - Настройте Menu Button в BotFather
   - Откройте бота в Telegram
   - Нажмите на кнопку меню (или отправьте команду `/start` и нажмите на кнопку)
   - TMA откроется внутри Telegram

## Дополнительные возможности

### Можно добавить:

1. **История запросов**: Сохранение предыдущих поисков
2. **Избранное**: Сохранение полезных источников
3. **Экспорт результатов**: Отправка результатов в чат
4. **Настройки**: Выбор поисковой системы, количества результатов
5. **Офлайн режим**: Кэширование результатов

## Troubleshooting

### TMA не открывается:
- Проверьте URL в BotFather (должен быть `https://findorigin.vercel.app/mini-app`)
- Убедитесь, что домен задеплоен на Vercel
- Проверьте, что скрипт Telegram Web App загружается (должен быть в `<head>`)

### Ошибки при поиске:
- Проверьте логи Vercel
- Убедитесь, что переменные окружения настроены (YANDEX_API_KEY, OPENAI_API_KEY, USE_OPENROUTER)
- Проверьте, что API endpoint `/api/mini-app/analyze` доступен

### UI не отображается правильно:
- Проверьте, что Telegram Web App API инициализирован
- Убедитесь, что темы применяются корректно
- Проверьте консоль браузера на ошибки

